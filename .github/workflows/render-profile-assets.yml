name: Validate profile SVG assets

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - assets/*.svg
      - .github/workflows/render-profile-assets.yml
      - README.md
  pull_request:
    branches:
      - main
    paths:
      - assets/*.svg
      - .github/workflows/render-profile-assets.yml
      - README.md

concurrency:
  group: validate-profile-svg-assets-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Validate SVG assets and README references
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate expected SVG files
        shell: bash
        run: |
          set -euo pipefail

          expected_svg_assets=(
            "assets/hero-banner.svg"
            "assets/section-divider.svg"
            "assets/project-mmwave-vislog.svg"
            "assets/project-local-device-portal.svg"
            "assets/project-microserver-deployment.svg"
          )

          for file in "${expected_svg_assets[@]}"; do
            echo "Validating $file"
            test -s "$file"
            grep -q '<svg' "$file"
            grep -q '</svg>' "$file"
            grep -q 'xmlns="http://www.w3.org/2000/svg"' "$file"

            if grep -q '{{' "$file"; then
              echo "::error file=$file::Template token remains in final SVG."
              exit 1
            fi

            if grep -qi '<foreignObject' "$file"; then
              echo "::error file=$file::foreignObject is not allowed in README SVG assets."
              exit 1
            fi
          done

      - name: Validate README asset references
        shell: bash
        run: |
          set -euo pipefail

          required_readme_refs=(
            './assets/hero-banner.svg'
            './assets/section-divider.svg'
            './assets/project-mmwave-vislog.svg'
            './assets/project-local-device-portal.svg'
            './assets/project-microserver-deployment.svg'
          )

          for ref in "${required_readme_refs[@]}"; do
            grep -q "$ref" README.md || {
              echo "::error file=README.md::Missing required SVG reference: $ref"
              exit 1
            }
          done

          if grep -q './assets/active-projects-card.svg' README.md; then
            echo "::error file=README.md::README still references stale combined active-projects-card.svg."
            exit 1
          fi

          if grep -q './assets/.*\.png' README.md || grep -q './assets/.*\.gif' README.md; then
            echo "::error file=README.md::README should only reference SVG profile assets."
            exit 1
          fi

  artifact:
    name: Package SVG assets
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Stage SVG artifact files
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p profile-svg-assets
          cp assets/hero-banner.svg profile-svg-assets/
          cp assets/section-divider.svg profile-svg-assets/
          cp assets/project-mmwave-vislog.svg profile-svg-assets/
          cp assets/project-local-device-portal.svg profile-svg-assets/
          cp assets/project-microserver-deployment.svg profile-svg-assets/

      - name: Upload SVG artifacts
        uses: actions/upload-artifact@v4
        with:
          name: profile-svg-assets
          path: profile-svg-assets/*.svg
          if-no-files-found: error
